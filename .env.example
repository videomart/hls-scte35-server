# Copie para .env e preencha com valores reais antes de subir o compose.
# .env não é versionado (ver .gitignore).

# Passphrase SRT (10-79 caracteres) — exigida do lado do TVPlay/cliente
# para publicar no scte-monitor. Sem isso, qualquer um na rede pode
# publicar/testar no streamid.
SCTE_SRT_PASSPHRASE=

# Credenciais HTTP Basic Auth da página do monitor (player + tabela de cues).
SCTE_WEB_USER=
SCTE_WEB_PASS=
